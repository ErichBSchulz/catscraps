[project]
name = "catscraps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
    "typer>=0.12.0",
    "rich>=13.0.0",
]

[project.scripts]
plot-benchmark = "catscraps.cli:app"
